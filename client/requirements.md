## Packages
framer-motion | Smooth animations for calculator transitions and history list
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Coordinate system logic (Rack/Shelf/Tray/Bin/Item) must be implemented in a utility file.
API endpoints defined in shared/routes.ts must be matched exactly.
